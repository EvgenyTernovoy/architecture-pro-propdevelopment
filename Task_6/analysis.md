# Отчёт по результатам анализа Kubernetes Audit Log

## Подозрительные события

1. Доступ к секретам:
   - Кто: kubernetes-admin
   - Где: kube-system
   - Почему подозрительно: Не подозрительно

2. Привилегированные поды:
   - Кто: minikube-user
   - Комментарий: RBAC разрешил создание пода. Под был создан с privileged: true и "serviceAccountName": "default" что позволяет использовать токен для доступа к API Kubernetes

3. Использование kubectl exec в чужом поде:
   - Не обнаружено

4. Создание RoleBinding с правами cluster-admin:
   - Не обнаружено

5. Удаление audit-policy.yaml:
   - Не обнаружено

## Вывод

Нам нужно ввести более строгие ограничения для политик RBAC, следовать принципу минимальных привилегий, выделить отдельные роли для создания и редактирования ресурсов внутри контейнера, а так же ограничить опирации чтения ролями.
